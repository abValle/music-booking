
<div class="container text-center">
  <div class="row align-items-center mt-5" id="profile-container">
    <h2>Como funcionam nossos serviços</h2>
  </div>

<div row>
  <div class="container text-center">
    <div class="row align-items-center mt-4" id="profile-container">
    <%= image_tag 'img_banner2.png' %>
    </div>
  </div>
</div>


<% if user_signed_in? %>
  <%= link_to "Eventos", events_path,  class: "btn btn-primary" if current_user.musician %>

  <%= link_to "Crie seu evento", new_company_event_path(@company),  class: "btn btn-primary" if current_user.company %>

  <%= link_to "Minhas Propostas Pendentes", proposals_path,  class: "btn btn-primary" if current_user.company %>
<% end %>
