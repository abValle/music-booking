<%= render 'banner'%>

<%= render 'home-carosel' unless user_signed_in? %>

<%= render 'banner_segundo' if !user_signed_in? || current_user.company %>

<%= render 'banner_terceiro' if !user_signed_in? || current_user.company %>

<%= render 'banner_quarto' if !user_signed_in? || current_user.musician %>

<%= render 'banner_quinto' if !user_signed_in? || current_user.musician %>
